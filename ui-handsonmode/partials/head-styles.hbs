    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Red+Hat+Display:wght@400;500;700&family=Red+Hat+Mono:wght@400;700&family=Red+Hat+Text:wght@400;700&display=swap">
    <link rel="stylesheet" href="{{{uiRootPath}}}/css/site.css">
    <style>
      :root { --hom-blue: #246BFE; --hom-ink: #13233D; }
      /* Light masthead: the wordmark is designed on white. */
      .hom-header { background: #ffffff !important; border-bottom: 3px solid var(--hom-blue); }
      .hom-header .navbar { background: transparent; }
      .hom-brand { padding-top: 0.35rem !important; padding-bottom: 0.35rem !important; }
      .hom-wordmark-img { height: 42px; width: auto; display: block; }
      .hom-tagline { color: #5a6a85 !important; font-size: 0.8rem; letter-spacing: 0.06em; align-self: center; }
      .hom-seat { color: var(--hom-ink) !important; font-size: 0.85rem; }
      .hom-header .navbar-link, .hom-header .navbar-item { color: var(--hom-ink); }
      .hom-cta { border: 2px solid var(--hom-blue); border-radius: 999px; color: var(--hom-blue) !important; padding: 0.15rem 0.95rem !important; margin: auto 0.75rem; font-size: 0.85rem; font-weight: 600; }
      .hom-cta:hover { background: var(--hom-blue); color: #ffffff !important; }
      .hom-footer { background: var(--hom-ink); color: #b8c2d4; border-top: 3px solid var(--hom-blue); padding: 1.1rem 1.5rem; font-size: 0.85rem; }
      .hom-footer a { color: #7ea5ff; }
    </style>
